import { PaymentObjectGroupRefs } from './payment-object-group-refs';

describe('PaymentObjectGroupRefs', () => {
  it('should create an instance', () => {
    expect(new PaymentObjectGroupRefs()).toBeTruthy();
  });
});
