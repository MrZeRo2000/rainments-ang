import { PaymentObjectTotals } from './payment-object-totals';

describe('PaymentObjectTotals', () => {
  it('should create an instance', () => {
    expect(new PaymentObjectTotals()).toBeTruthy();
  });
});
