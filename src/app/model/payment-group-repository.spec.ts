import { PaymentGroupRepository } from './payment-group-repository';

describe('PaymentGroupRepository', () => {
  it('should create an instance', () => {
    expect(new PaymentGroupRepository()).toBeTruthy();
  });
});
