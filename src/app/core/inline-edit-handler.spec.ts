import { InlineEditHandler } from './inline-edit-handler';

describe('InlineEditService', () => {
  it('should create an instance', () => {
    expect(new InlineEditHandler()).toBeTruthy();
  });
});
