import { ShortMonthYearPipe } from './short-month-year.pipe';

describe('ShortMonthYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortMonthYearPipe();
    expect(pipe).toBeTruthy();
  });
});
