import { PaymentObjectTotalsRepository } from './payment-object-totals-repository';

describe('PaymentObjectTotalsRepository', () => {
  it('should create an instance', () => {
    expect(new PaymentObjectTotalsRepository()).toBeTruthy();
  });
});
