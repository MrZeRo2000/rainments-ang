import { AppPersistRepository } from './app-persist-repository';

describe('AppPersistRepository', () => {
  it('should create an instance', () => {
    expect(new AppPersistRepository()).toBeTruthy();
  });
});
