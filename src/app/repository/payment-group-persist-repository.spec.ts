import { PaymentGroupPersistRepository } from './payment-group-persist-repository';

describe('PaymentGroupPersistRepository', () => {
  it('should create an instance', () => {
    expect(new PaymentGroupPersistRepository()).toBeTruthy();
  });
});
