import { PaymentObjectPersistRepository } from './payment-object-persist-repository';

describe('PaymentObjectPersistRepository', () => {
  it('should create an instance', () => {
    expect(new PaymentObjectPersistRepository()).toBeTruthy();
  });
});
