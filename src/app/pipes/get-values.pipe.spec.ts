import { GetValuesPipe } from './get-values.pipe';

describe('GetValuesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GetValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
