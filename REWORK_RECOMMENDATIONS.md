# Rework Recommendations — rainments-ang

| | |
|---|---|
| **Generated** | 2026-05-27 |
| **Last updated** | 2026-06-17 |
| **Generated by** | Claude Opus 4.7 (Anthropic), via Claude Code |
| **Requested by** | roman.pulov@sixt.com |
| **Scope** | Architecture, code organization, and tooling review of the `rainments-ang` Angular project |

> Snapshot of the codebase at the date above. Some items may have been addressed already (Karma → Vitest, Protractor → Playwright, standalone-bootstrap migration, dead-module cleanup, Sass-deprecation analysis). Re-evaluate before acting on a stale entry.
>
> **2026-06 update:** an incremental signals migration is underway — several components (payment-objects/groups/products tables, payments-dashboard, payments-master, payments-date-selection, payments-table + summary, update-payment-group, backup-database) now use signal-based `ReadRepository`/`CrudRepository` injected via tokens (`repository-tokens.ts`), typed forms, `input()`/`output()`, and shared validators (`core/validators/form-validators.ts`). Several `§2`/`§3` entries that describe the old `UntypedFormBuilder` / `getLoadSuccessObservable` / per-entity-repository-class state are now partially stale for those components. App-level Zone removal is still pending. §2.17 below was written against this new signal-based baseline.

> **Project convention:** do not suppress build warnings (Sass deprecations, ESLint, TS, schematics). Fix at root where possible, otherwise leave visible — the warning is the signal that upstream has moved.

---

## 1. What's good

1. **Clear top-level layering.** [src/app/](src/app/) splits into `model`, `repository`, `data-source`, `config`, `messages`, `core`, `components` — each layer has a single responsibility and dependencies point one way (components → repository → data-source → http).
2. **Reusable generic base classes.** [core/repository/read-repository.ts](src/app/core/repository/read-repository.ts), [read-write-repository.ts](src/app/core/repository/read-write-repository.ts), and [core/table/common-editable-table-component.ts](src/app/core/table/common-editable-table-component.ts) capture CRUD + table editing patterns once. Per-entity classes like [payment-repository.ts](src/app/repository/payment-repository.ts) collapse to a few lines.
3. **A real `core/` sub-tree.** Pipes, directives, modal helpers, drag, inline-edit, date utilities, color utilities — all framework-agnostic and reusable across siblings. This is genuinely the strongest part of the codebase.
4. **CQRS-lite separation.** Read repositories and persist repositories are decoupled (`*-repository` + `*-persist-repository`), sharing a `Subject` so a successful write triggers a reload everywhere.
5. **Centralized HTTP entry point.** [rest-data-source.ts](src/app/data-source/rest-data-source.ts) is the only place that knows about REST URLs; [configuration.ts](src/app/config/configuration.ts) uses an `InjectionToken` swapped at build-time via `fileReplacements`. Idiomatic.
6. **Test discipline.** Nearly every file has a `.spec.ts` companion. The pure-logic ones ([date-range-generator](src/app/core/utils/date-range-generator.spec.ts), [payment-utils](src/app/utils/payment-utils.spec.ts)) actually exercise behavior.
7. **Template uses modern `@if`/`@for`** control flow consistently.

---

## 2. What's not so good

### 2.1 Outdated Angular patterns despite v21 dependencies
- ~~Every component carries `standalone: false`.~~ **Done 2026-05-29:** all components and directives are now standalone; `AppModule` and `AppRoutingModule` deleted; bootstrap uses `bootstrapApplication(AppComponent, appConfig)`; `inject()` adopted in most components.
- **Still pending:** no `signal()` / `computed()` / `effect()`, no `httpResource`, no typed forms, no new Router APIs (data resolvers, functional guards).
- ZoneJS is still in use; templates call methods like `getSelectablePayments()` and `getPrevPeriodProductCounter(...)` on every CD cycle, with no `OnPush`.
- `UntypedFormBuilder` / `UntypedFormGroup` used throughout [payments-table.component.ts:111-117](src/app/components/payments-table/payments-table.component.ts#L111-L117) — typed forms (Angular 14+) would catch the dozens of string-based `controls.product.value` accesses.

### 2.2 Empty / vestigial modules
- ~~[model/model.module.ts](src/app/model/model.module.ts) — empty NgModule, declares nothing.~~ **Deleted 2026-05-29.**
- ~~[config/config.module.ts](src/app/config/config.module.ts) — exists only to provide one token.~~ **Deleted 2026-05-29;** `RestUrlEnv` now provided directly in `app.config.ts`.
- ~~[data-source/data-source.module.ts](src/app/data-source/data-source.module.ts) — dead, never imported; `RestDataSource` was already provided via `RepositoryModule`.~~ **Deleted 2026-05-29.**
- **Still candidates for removal:**
  - [repository/repository.module.ts](src/app/repository/repository.module.ts) — pure provider list. Each of the ~20 repositories uses bare `@Injectable()`; adding `providedIn: 'root'` to each lets the whole module be deleted. Low risk, ~50 LOC removed.
  - [font-awesome-icons/font-awesome-icons.module.ts](src/app/font-awesome-icons/font-awesome-icons.module.ts) — registers FA icons via a constructor side effect. Replace with `provideAppInitializer(() => inject(FaIconLibrary).addIcons(...))` in `app.config.ts`; removes the last bespoke NgModule.

### 2.3 Repository class explosion
About 40 repository files. Most `*-persist-repository.ts` are 6-line constructors (see [payment-persist-repository.ts](src/app/repository/payment-persist-repository.ts)). A generic factory keyed by `resourceName`, or a single `provideRepository<T>(name)` helper, could collapse ~80% of these into configuration data.

### 2.4 Leaky reactive plumbing
- `ReadRepository.data: T[]` is mutated in place via `splice` ([read-repository.ts:70-74](src/app/core/repository/read-repository.ts#L70-L74)); consumers must poll via `getData()`.
- `loadSuccess`, `persistSuccess`, `loadingState` are exposed as raw `Subject<T>` ([persist-repository.ts:18-28](src/app/core/repository/persist-repository.ts#L18-L28)) — consumers can call `.next()` on them. Should be `asObservable()`, and for `loadingState` a `BehaviorSubject` (late subscribers miss the loading=true edge).
- Subscriptions inside repositories ([read-write-repository.ts:19](src/app/core/repository/read-write-repository.ts#L19)) are never torn down.

### 2.5 No HTTP interceptors / no typed error model
Errors are stringified and shoved into `MessagesService` (`'Network error:' + ...`). No `catchError`, no retry, no auth header injection, no global loading interceptor. Adding one interceptor would eliminate dozens of repeated error blocks.

### 2.6 Heavy components doing too much
[payments-table.component.ts](src/app/components/payments-table/payments-table.component.ts) is ~500 lines: form builder, inline-edit orchestration, validation, rounding, clipboard paste, color emission, and ad-hoc previous-period diffing. Several private `static` math helpers belong in `core/utils`.

### 2.7 Constructor-based component inheritance taking a runtime constructor
[common-editable-table-component.ts:25](src/app/core/table/common-editable-table-component.ts#L25) accepts `ctor: new() => W`. This pattern fights both DI and TS — composition (a `useFactory` provider, or a `signal()`-based store) would be cleaner.

### 2.8 No lazy loading
[app.routes.ts](src/app/app.routes.ts) (formerly `app-routing.module.ts`, migrated 2026-05-29) still eagerly imports every feature component. With the production build warning at 2MB initial bundle, route-level lazy loading is low-hanging fruit — and now trivial with standalone components: `loadComponent: () => import('./...').then(m => m.SomeComponent)`.

### 2.8.1 `withHashLocation()` workaround for Tomcat 404s
[app.config.ts](src/app/app.config.ts) uses `provideRouter(routes, withHashLocation())`. The reason (per developer): deep-linking to a routed path like `/rainments-ang/payments/123` on Tomcat returns 404, because Tomcat looks for a static file at that path. Hash routing sidesteps this by encoding the route in the URL fragment (`/#/payments/123`), which the server never sees. **Constraint:** server-level Tomcat configuration is not always available (shared / managed instances).

**Costs of the current workaround:**
- Ugly URLs (`/#/...`); not friendly to share/copy
- The URL fragment is unavailable for in-page anchors (router owns it)
- Server access logs don't show the route, only `/`
- SSR / prerender / SEO is essentially impossible
- The `#` confuses many external tools (e-mail clients, ticketing systems, link previewers)

**Deployment topology (as of 2026-05-29):**
- **`rainments-ang`** is built via `ng build --base-href=/rainments-ang/` and deployed as **exploded static files** to `<tomcat>/webapps/rainments-ang/` (via `common/builder/builder.psm1::Start-DeployWeb`). It is *not* packaged as a WAR file, but Tomcat treats any directory under `webapps/` as a webapp — so you CAN place a `WEB-INF/web.xml` inside that directory and Tomcat will honor it.
- **`rainments-wss`** is a Spring Boot WAR (`bootWar`) deployed to `<tomcat>/webapps/rainments-wss.war`. Frontend and backend live at separate context paths today; CORS is enabled in [WebConfig.java](../rainments-wss/src/main/java/com/romanpulov/rainmentswss/config/WebConfig.java) to bridge them.

Given that topology, there are two real options to retire `withHashLocation()`:

---

#### Option A — minimal: add `WEB-INF/web.xml` to the frontend's deployment directory

Keep the current two-deployable architecture. Just add a `web.xml` template to `rainments-ang/` and have the deploy script copy it into `<tomcat>/webapps/rainments-ang/WEB-INF/` alongside the static files.

**Steps:**

1. Create `rainments-ang/webapp-templates/WEB-INF/web.xml`:
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                                https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
            version="6.0">
     <error-page>
       <error-code>404</error-code>
       <location>/index.html</location>
     </error-page>
   </web-app>
   ```
   (Tomcat 9.x: use the older `http://xmlns.jcp.org/xml/ns/javaee` namespace and `version="4.0"`. The `<error-page>` element itself is stable since Servlet 2.3.)

2. Update [common/builder/builder.psm1](../common/builder/builder.psm1) `Start-DeployWeb` to also copy the WEB-INF folder:
   ```powershell
   $WebInfSrc = "$PSScriptRoot../../../$project/webapp-templates/WEB-INF"
   if (Test-Path $WebInfSrc) {
     Copy-Item -Path $WebInfSrc -Destination "$DestinationFolder\" -Recurse -Force
   }
   ```

3. Drop `withHashLocation()` from `app.config.ts`: `provideRouter(routes)`.

**Caveat — HTTP status remains 404** (body is `index.html`, so browsers render correctly, but monitoring/CDN layers may misinterpret). To return 200, a tiny servlet filter would need to be packaged with the frontend deploy — which means introducing a Java build step into the otherwise pure-static frontend project. Likely not worth it; if status code matters, prefer Option B.

**Pros:** Minimal change, backend WAR untouched, half-day of work.
**Cons:** Frontend gains a hidden Java-EE artifact; 404 status not fixable without introducing a Java compile step into the frontend.

---

#### Option B — cleaner: merge the frontend into the Spring Boot WAR

Long-term better: serve the Angular app from the same Spring Boot WAR as the API. Spring Boot has first-class SPA support and the Tomcat plumbing is already there.

**Steps:**

1. **In `rainments-ang`:** change the build to output Angular's `dist/` into Spring's static path. Either set Angular's `outputPath` in `angular.json`, or add a gradle copy task in `rainments-wss/build.gradle`:
   ```gradle
   def angularDist = file("../rainments-ang/dist/rainments-ang/browser")

   tasks.register('copyAngular', Copy) {
       from angularDist
       into file("src/main/resources/static")
       onlyIf { angularDist.exists() }
   }
   processResources.dependsOn copyAngular
   ```
   Add `src/main/resources/static/` to `.gitignore`.

2. **Adjust the base-href** in the Angular build command (in `deployWebProject.ps1`) from `--base-href=/rainments-ang/` to `--base-href=/rainments-wss/` (or whatever the combined context becomes). Or strip the trailing prefix entirely if the combined WAR is deployed as ROOT.

3. **Add `SpaConfig.java`** in `rainments-wss/src/main/java/com/romanpulov/rainmentswss/config/`:
   ```java
   @Configuration(proxyBeanMethods = false)
   public class SpaConfig {
       @Bean
       public ErrorPageRegistrar spaErrorPageRegistrar() {
           return registry -> registry.addErrorPages(
               new ErrorPage(HttpStatus.NOT_FOUND, "/index.html")
           );
       }
   }
   ```
   This is the Spring Boot equivalent of `<error-page>` in `web.xml`. **Same 404-status caveat applies.** A path-regex-based fallback `@Controller` could return 200, but maintaining the exclusion list (`/payments`, `/payment-objects`, `/payment-groups`, `/products`, `/app:*`, `/app-info`, `/hello`) is brittle — keep the `ErrorPageRegistrar` unless the 404 status hurts monitoring.

4. **Drop CORS** from [WebConfig.java](../rainments-wss/src/main/java/com/romanpulov/rainmentswss/config/WebConfig.java) — no longer needed when frontend and backend share an origin.

5. **Drop `withHashLocation()`** from `app.config.ts`.

6. **Update deployment scripts** to skip the separate frontend deploy step.

**URL-collision check** — backend already has `GET /payments`, `GET /payment-groups`, `GET /payment-objects`, `GET /products`, `GET /app-info`, `GET /hello`, `GET /app:*`, plus `{PUT,DELETE}/{id}` per-entity. The Angular route `/payments/:id` does NOT collide with any backend `GET /payments/{id}` handler (none exists — only PUT/DELETE on that path). So `GET /rainments-wss/payments/123` → no controller match → 404 → SpaConfig forwards to `/index.html` → Angular boots → routes to `/payments/123` ✓. Then Angular's own AJAX calls go to `GET /payments` (the list), which IS a controller — works normally. The architecture is collision-safe today, but new backend `GET /payments/{id}`-style routes would need to be added carefully (or moved under an explicit `/api/` prefix).

**Pros:** One deployable; no CORS; Java-side configurability for SPA fallback, auth integration, etc.; standard Spring Boot pattern.
**Cons:** ~half-day to wire up; frontend and backend deploys become coupled; Spring Boot's executable WAR layout puts static at `BOOT-INF/classes/static/` (handled automatically by `processResources`, but worth verifying).

---

#### Important distinction: plain WAR vs Spring Boot executable WAR

The `bootWar` plugin produces a **Spring Boot executable WAR** with non-standard layout (`BOOT-INF/classes/static/` for resources instead of the root). Do NOT add a `<error-page>` to a hand-written `WEB-INF/web.xml` inside the Spring Boot WAR — use `ErrorPageRegistrar` instead (that's the bean Spring Boot translates into the equivalent server-level registration). Plain-WAR `web.xml` works for the **frontend exploded directory** (Option A) because that's not a Spring Boot artifact.

---

#### Recommendation

- **Option A** if the two-deployable architecture is fine and you want the smallest possible change.
- **Option B** if you're willing to invest a half-day to simplify the deployment model long-term. The fact that the backend already has the right shape and your CORS is currently a workaround makes Option B the more honest answer to "what should this look like."

**Fallback if neither option is workable** (e.g., dropping pure-static files into `webapps/ROOT/` with no `WEB-INF/` access): a service worker that serves `index.html` for `navigate` fetch failures. Only helps on the 2nd visit (SW must be installed first); first-visit cold-start deep links still 404. Not a real substitute.

### 2.9 State is duplicated across repositories
Each repository holds its own `data: T[]`. Two components needing the same `PaymentRefs` each trigger a fresh HTTP call. A signal store (or NgRx Component Store) shared across `payments-master` ↔ `payments-table` ↔ `payments-summary` would remove redundant fetches and the `getLoadSuccessObservable` choreography.

### 2.10 Weak/no type safety at REST boundary
`getResponse(...)`, `postResponse(...)` all return `Observable<HttpResponse<any>>`. `any` leaks into every repository and out through `getData()`. A typed `RestDataSource<T>` (or `httpResource<T>()`) would surface schema drift at compile time.

### 2.11 Bugs and smells
- `headers.append('Content-Type', undefined)` in [rest-data-source.ts:37](src/app/data-source/rest-data-source.ts#L37) — invalid header; for `FormData` you just don't set Content-Type.
- `Number.parseInt(x, 0)` used widely (e.g. [payments-master.component.ts:26](src/app/components/payments-master/payments-master.component.ts#L26)) — radix `0` accepts `0x...` and `0o...`; should be `10`.
- `console.log('load data from ' + ...)` ([read-repository.ts:56](src/app/core/repository/read-repository.ts#L56)) and `console.log('constructing PersistRepository')` left in production code paths.
- Typo: folder [components/data-managment/](src/app/components/data-managment/) (missing 'e'); method `pervPeriodCounterLabelClick` ([payments-table.component.ts:486](src/app/components/payments-table/payments-table.component.ts#L486)).

### 2.12 Tests mostly verify "the framework still works"
Most `.spec.ts` files only check `expect(component).toBeTruthy()` while pulling in the whole `RepositoryModule` + `CoreModule`. They cost CI minutes without asserting behavior. The valuable specs (`date-range-generator`, `payment-utils`, `payments-color-utils`) prove the pattern works — extend it, drop the smoke tests.

> **Note:** the Karma → Vitest migration exposed that several of these smoke tests were not just trivial but partially broken (they passed only because Karma's loose defaults silenced unknown-element/property/iterator errors). Two have been marked `it.skip` pending real fixes.

### 2.13 Dead/legacy tooling
- ~~Protractor + `app.e2e-spec.ts`~~ — replaced with Playwright on 2026-05-27.
- ~~Karma + Jasmine~~ — replaced with Vitest on 2026-05-27.
- `karma-cli` was sitting in `dependencies` instead of `devDependencies` (removed in same migration). `webpack` is still in `dependencies` and should move to `devDependencies` if needed at all.

### 2.14 README is a personal scratchpad
[README.md](README.md) mixes "install bootstrap 5" notes, version upgrade commands, and migration breadcrumbs. There's no architecture overview, no domain glossary (Payment / PaymentObject / PaymentGroup / PaymentRefs / PaymentRep), no docs on running `server/`. New contributors would be lost.

### 2.15 Sass `@import` is deprecated (will be removed in Dart Sass 3.0.0)
[src/styles.scss](src/styles.scss) uses three `@import`s. Two are upstream (`bootstrap/scss/bootstrap`, `ngx-bootstrap/datepicker/...`) and one is own code (`_variables.scss`). Recommended migration:

1. **Convert the own-code import:** `@import 'variables';` → `@use 'variables';` (variables become namespaced as `variables.$primary`, etc.).
2. **Convert the Bootstrap import to `@use ... with (...)`** — Bootstrap 5.3.8 (currently installed) supports the module syntax properly:
   ```scss
   @use 'bootstrap/scss/bootstrap' as bootstrap with (
     $primary: variables.$primary,
     $tooltip-arrow-color: variables.$tooltip-arrow-color,
     $tooltip-bg: variables.$tooltip-bg,
     $tooltip-color: variables.$tooltip-color
   );
   ```
   This replaces the old "define `!default`-overrides before importing" timing trick with an explicit override list.
3. **Namespace-prefix Bootstrap variable references** in the rest of `styles.scss`: `$border-radius` → `bootstrap.$border-radius`, `$table-cell-padding-y`, `$table-border-color`, `$btn-border-radius` (lines 89, 90, 100, 101, 121 of the current file).
4. **`node_modules/` path prefix is optional** — Angular's Sass loader has `node_modules` on its load path; `@use 'bootstrap/scss/bootstrap'` resolves the same as the longer form.
5. **ngx-bootstrap stays as `@import`** — `ngx-bootstrap` 19.x still uses `@import` internally; its deprecation warning is upstream-controlled. **Leave it visible** (per project convention) — when it disappears, that's the signal upstream has migrated and you can `@use` it too.
6. **Possibly-dead variable:** `$fa-font-path` in [_variables.scss](src/_variables.scss) — grep before keeping; no current SCSS file imports the FontAwesome SCSS, so it may be orphaned.

The automated migrator (`npx sass-migrator module src/styles.scss`) does step 1 mechanically. Steps 2-3 require manual care because they change variable-resolution semantics.

### 2.16 Standalone-component DI gotcha: pipes used via `inject()`
A standalone component that uses a pipe **only in its template** needs the pipe class in `imports: [...]`. A component that **injects the pipe class** (e.g., `private p = inject(AmountPipe)` for programmatic formatting) needs the pipe additionally in `providers: [...]`. Under the old NgModule setup, declaring a pipe in `CoreModule.declarations` covered both cases — with standalone, you opt in per component.

Current state: only [payments-table.component.ts:87](src/app/components/payments-table/payments-table.component.ts#L87) does this (`inject(AmountPipe)`); fixed 2026-05-29 by adding `providers: [AmountPipe]`. If similar `inject(...Pipe)` patterns are introduced later, remember this rule — the error is `NG0201: No provider found for _XxxPipe`.

### 2.17 Full-table reload on every write — per-row update opportunity

**Context (post-signals-migration).** Every successful CRUD or inline PATCH routed through [common-editable-table-component.ts](src/app/core/table/common-editable-table-component.ts) `crudData$` calls `loadRepositoryData()` — refetching the whole list (or the whole `PaymentRefs` composite) and re-running its post-processing (e.g. the `mapPaymentRefs` prev-period re-link + summary recompute). The desired UX is: the row being updated/deleted shows a loading indicator, **only that row visibly updates** on success, and a full reload happens **only on error**.

There are two ways to get there; **the cheap one is recommended.**

#### Recommended — keep the reload, lean on `@for … track <id>`

Angular's `track` keys rows by identity: a same-`id` row reuses its existing `<tr>` DOM node and only the changed cell bindings repaint; new/removed ids add/remove rows. So **even with a full refetch, unchanged rows don't flicker and only the changed row updates visibly** — which is the actual goal. Note `track` does not skip the HTTP call and does not content-diff; it controls DOM-node reuse, and normal change detection handles the per-binding update.

Work required (small, no core-repository rework):
1. **Fix track keys.** [payments-table.component.html](src/app/components/payments-table/payments-table.component.html) still tracks the wrapper object — `@for (item of selectableItems(); track item)` → `track item.value.id`. The rows are rebuilt as new `SelectableItem` wrappers on each load, so object-identity tracking recreates every row; tracking the underlying `Payment.id` reuses them. (The simple tables already use `track item.id`.)
2. **Per-row pending — component-level only.** Add a `pendingRowId` signal (or small `Set`); set it right before `crudRepository.execute(...)` for Update/Delete/inline-Patch, clear it on the `crudData$` outcome. No `CrudRepository`/`ReadRepository` change.
3. **Row-loading in the template.** `@if (isRowPending(item)) { <app-core-row-loading [colspan]="…"/> } @else { …row… }`, backed by a small shared `CoreRowLoadingComponent` (reuse `LoadingSpinnerElementComponent`). For payments-table, **revert the "hide the whole table while `loadingSignal()`" gate** back to showing the table with a per-row spinner.
4. **Errors** already trigger a full reload — leave as is.

Cost: one HTTP round-trip + read-repo post-processing per write (negligible at these table sizes). Meets all four goals with the least code and risk.

#### Heavier alternative — true per-row mutation (no reload)

Avoids the round-trip entirely, at materially more code/risk:
- `ReadRepository`: back `dataSignal` with a writable signal and add an `update((current) => next)` mutation API.
- `CommonEditableTableComponent`: an overridable `applyCrudResult(action, result)` — default upsert/remove-by-id for simple `R = W` tables; a custom one for payments that replaces the row in `paymentList`, re-links `prevPeriodPayment`, and lets the `computed` summary recompute.
- `CrudRepository`: correlate each emitted result with its originating action, and switch `switchMap` → `mergeMap` so independent rows can be in flight at once.

**Backend response shapes (verified against `rainments-wss`):** the generic `AbstractServiceRestController` POST/PUT return the full DTO and DELETE returns an empty body; **the payments inline PATCH (`PaymentController.partialUpdate`) returns `RowsAffectedDTO`, not the `Payment`.** So the no-reload variant must apply the value the *client* sent for inline edits (it cannot rebuild the row from the response). The track-id variant sidesteps this because it refetches.

#### Recommendation

Do the **track-id + per-row-loading** version. Reserve the per-row-mutation rework for when avoiding the reload round-trip itself becomes a real requirement (large lists, high write frequency, or offline-ish UX).

**Caveat:** with the current `switchMap` in `CrudRepository`, rapid back-to-back row edits serialize — a second op cancels the first's request. Fine for one-cell-at-a-time inline editing; only revisit `mergeMap` + result↔action correlation if genuinely concurrent row edits are needed.

---

## 3. Proposed improvements, ranked by leverage

| # | Change | Why |
|---|--------|-----|
| 1 | ~~**Migrate to standalone**~~ + signals + typed forms incrementally. Start at leaves (pipes, simple components). | **Standalone done 2026-05-29** (bootstrap, all components, all directives, AppModule + AppRoutingModule deleted; per-spec `import '../../app.module';` workaround no longer needed). **Signals / typed forms still pending.** |
| 2 | **Replace `*-persist-repository.ts` boilerplate with one generic `createRepository<T>(name)` factory.** | Deletes ~30 trivial files; new entities become one-liners. |
| 3 | **Introduce an `HttpInterceptor` for errors + loading.** Funnel into a single `NotificationService` (rename `MessagesService`). | Removes duplicated `catchError`/message code from every repository; consistent UX. |
| 4 | **Add route-level lazy loading** for `/payments/:id` and `/reports/:id`. Drop `useHash: true` if not required. | Cuts initial bundle, easier code-splitting. |
| 5 | **Adopt a shared signal-based store** (Angular `signal()` + `computed()`, or NgRx SignalStore) for `PaymentRefs` and `PaymentRep`. | Removes the `loadSuccess` subject choreography and double-fetching. |
| 6 | **Type the REST surface.** `RestDataSource.get<T>(...)` and `httpResource<T>()` for read-models. Generate types from the backend if possible. | Compile-time guarantees instead of `any`. |
| 7 | **Replace `Subject` with `BehaviorSubject` / `signal`** for `loading` and `loadingState`; expose via `asObservable()` / readonly signals. | Fixes the late-subscriber bug and the "subjects leak `.next()`" issue. |
| 8 | **Split `payments-table.component.ts`** into: table view, inline-edit controller, form host, summary host. Move static math helpers to `core/utils/numeric.ts`. | The 500-line file is the project's biggest comprehension cost. |
| 9 | ~~**Delete dead modules**~~ (`ModelModule`, `ConfigModule`, `DataSourceModule` deleted 2026-05-29). **Remaining:** convert `RepositoryModule` (~20 repos → `providedIn: 'root'`) and `FontAwesomeIconsModule` (→ `provideAppInitializer`) to remove the last two NgModules. Also: trim the trivial `toBeTruthy()` smoke specs, convert remaining specs to assert real behavior; un-skip the two `it.skip`-ed specs. | Less to maintain; signal of intent. Also gets test count back to honest. |
| 10 | ~~Replace Protractor with Playwright; replace Karma with Vitest.~~ | **Done 2026-05-27.** |
| 11 | **Strip `console.log`** and fix small bugs: `parseInt(..., 10)`, the `Content-Type: undefined` line, the `data-managment` / `pervPeriod` typos (do these in a single rename PR). | Hygiene; the typo rename has a ripple across selectors so worth doing once. |
| 12 | **Rewrite the README** as: architecture diagram, domain glossary, how to run the json-server, deploy steps. Move upgrade history into a `CHANGELOG.md` or git tags. | Lowers onboarding cost considerably. |
| 13 | **Add CI** (lint + build + test) and a Prettier/ESLint config check on PR. Add `husky` + `lint-staged` for local enforcement. | Currently nothing enforces consistency. |
| 14 | **Reconsider `webpack` in `dependencies`** — move to `devDependencies` or remove. | Trims install size, reflects intent. |
| 15 | **Watch the `@angular/animations` deprecation.** `provideAnimationsAsync` (used in `src/test-setup.ts` to support ngx-bootstrap modals under tests) is deprecated; Angular plans to remove the imperative animations package in v23. Track ngx-bootstrap's migration or replace it. | Avoids a surprise upgrade blocker. |
| 16 | **Sass `@import` → `@use` migration.** Convert `src/styles.scss` to `@use 'variables'` + `@use 'bootstrap/scss/bootstrap' as bootstrap with (...)` (Bootstrap 5.3.8 supports it). Namespace-prefix Bootstrap variable references (`$border-radius` → `bootstrap.$border-radius`, etc.). Leave the ngx-bootstrap `@import` until upstream migrates — do NOT add `silenceDeprecations`. See §2.15 for the full diff. | Removes the user-controlled Sass deprecation warning; primes the codebase for Dart Sass 3.0.0. |
| 17 | **Replace `withHashLocation()`.** Two paths, see §2.8.1 for full recipes: **(A)** drop a `WEB-INF/web.xml` with `<error-page>` into the frontend's exploded webapp directory (minimal change, ~half-day); or **(B)** merge the Angular `dist/` into the Spring Boot WAR and add a `@Bean ErrorPageRegistrar` in `rainments-wss` (cleaner architecture, eliminates CORS, ~half-day plus testing). | Restores clean URLs, real server logs, SSR/SEO readiness; removes the `#`-fragment friction. Option B additionally collapses to one deployable and removes the CORS workaround in `WebConfig.java`. |
| 18 | **Per-row update on writes instead of full-table reload.** Cheap path: fix `@for` track keys to `id` (payments-table uses `track item` → `track item.value.id`), add a `pendingRowId` signal + a shared row-loading component, render a spinner on the in-flight row, keep full reload only on error. See §2.17. Heavier path (no reload at all) reserved for later. | Removes table-wide flicker on edit/delete/inline-patch; single visible row update; reuses existing plumbing (no `CrudRepository`/`ReadRepository` rework). |
